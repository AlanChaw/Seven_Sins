- hosts: subnode
  remote_user: ubuntu
  gather_facts: true

  roles:
    - role: subnode-couchdb

- hosts: masternode
  remote_user: ubuntu
  gather_facts: true

  roles:
    - role: masternode-couchdb
