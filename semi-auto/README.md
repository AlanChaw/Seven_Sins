these scripts are used on the **different** nodes.

- on the `sub-nodes`:

```bash
./subnode.sh
```

- on the `master-node`:

```bash
./masternode.sh
```

then the distributed couchdb clusters will be set.
